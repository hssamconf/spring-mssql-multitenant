app.name= "Multitenant"

#Hibernate can automatiquely detect the driver and the dialect

spring.datasource.url=jdbc:sqlserver://192.168.1.29:1433;databaseName=central_multitenant
spring.datasource.username=sa
spring.datasource.password=Casa123++
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto = update
# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=50
# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true

jwt.header= Authorization
jwt.expires_in= 86400
jwt.secret= multitenant
jwt.cookie= AUTH-TOKEN